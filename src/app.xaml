<?xml version="1.0" encoding="utf-8"?>
<Application x:Class="FGOLocalPlatform.App" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:fgolocalplatform="clr-namespace:FGOLocalPlatform;assembly=FGOLocalPlatform" StartupUri="MainWindow.xaml">
  <Application.Resources>
    <ResourceDictionary>
      <SolidColorBrush x:Key="PanelBrush" Color="#121016" />
      <SolidColorBrush x:Key="PanelRaisedBrush" Color="#1B1720" />
      <SolidColorBrush x:Key="AccentBrush" Color="#8A2E78" />
      <SolidColorBrush x:Key="AccentSoftBrush" Color="#4A1D46" />
      <Style x:Key="{x:Type ContextMenu}" TargetType="{x:Type ContextMenu}">
        <Setter Property="FontFamily" Value="Segoe UI, Microsoft YaHei UI" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Foreground" Value="#EDE7EF" />
        <Setter Property="Background" Value="#17131B" />
        <Setter Property="BorderBrush" Value="#5A3154" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="4" />
        <Setter Property="FrameworkElement.UseLayoutRounding" Value="True" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="{x:Type ContextMenu}">
              <Border Background="{TemplateBinding Control.Background}" BorderBrush="{TemplateBinding Control.BorderBrush}" BorderThickness="{TemplateBinding Control.BorderThickness}" Padding="{TemplateBinding Control.Padding}" CornerRadius="6">
                <ItemsPresenter />
              </Border>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>
      <Style x:Key="{x:Type MenuItem}" TargetType="{x:Type MenuItem}">
        <Setter Property="Foreground" Value="#EDE7EF" />
        <Setter Property="Background" Value="#00FFFFFF" />
        <Setter Property="Padding" Value="10,8" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="{x:Type MenuItem}">
              <Border Name="MenuSurface" Background="{TemplateBinding Control.Background}" Padding="{TemplateBinding Control.Padding}" CornerRadius="4">
                <Grid>
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                  </Grid.ColumnDefinitions>
                  <ContentPresenter ContentSource="Icon" VerticalAlignment="Center" Margin="0,0,8,0" />
                  <ContentPresenter Grid.Column="1" ContentSource="Header" RecognizesAccessKey="True" VerticalAlignment="Center" />
                  <TextBlock Grid.Column="2" Text="{TemplateBinding MenuItem.InputGestureText}" Margin="16,0,0,0" Foreground="#BFB5C2" />
                  <Popup Name="PART_Popup" IsOpen="{TemplateBinding MenuItem.IsSubmenuOpen}" Placement="Right" AllowsTransparency="True" Focusable="False">
                    <Border Background="#17131B" BorderBrush="#5A3154" BorderThickness="1" Padding="4" CornerRadius="6">
                      <ItemsPresenter />
                    </Border>
                  </Popup>
                </Grid>
              </Border>
              <ControlTemplate.Triggers>
                <Trigger Property="MenuItem.IsHighlighted" Value="True">
                  <Setter TargetName="MenuSurface" Property="Border.Background" Value="#4A1D46" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                  <Setter TargetName="MenuSurface" Property="UIElement.Opacity" Value="0.45" />
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>
      <Style x:Key="{x:Type ToolTip}" TargetType="{x:Type ToolTip}">
        <Setter Property="FontFamily" Value="Segoe UI, Microsoft YaHei UI" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Foreground" Value="#EDE7EF" />
        <Setter Property="Background" Value="#1B1720" />
        <Setter Property="BorderBrush" Value="#5A3154" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="10,8" />
        <Setter Property="MaxWidth" Value="420" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="{x:Type ToolTip}">
              <Border Background="{TemplateBinding Control.Background}" BorderBrush="{TemplateBinding Control.BorderBrush}" BorderThickness="{TemplateBinding Control.BorderThickness}" Padding="{TemplateBinding Control.Padding}" CornerRadius="6">
                <ContentPresenter>
                  <FrameworkElement.Resources>
                    <ResourceDictionary>
                      <Style x:Key="{x:Type TextBlock}" TargetType="{x:Type TextBlock}">
                        <Setter Property="TextWrapping" Value="Wrap" />
                      </Style>
                    </ResourceDictionary>
                  </FrameworkElement.Resources>
                </ContentPresenter>
              </Border>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>
      <Style x:Key="{x:Type Window}" TargetType="{x:Type Window}">
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FrameworkElement.UseLayoutRounding" Value="True" />
        <Setter Property="TextOptions.TextFormattingMode" Value="Display" />
        <Setter Property="Background" Value="#0C0A0E" />
        <Setter Property="Foreground" Value="#FFFFFFFF" />
        <Setter Property="FontFamily" Value="Segoe UI, Microsoft YaHei UI" />
      </Style>
      <Style x:Key="{x:Type Button}" TargetType="{x:Type Button}">
        <Setter Property="Margin" Value="3" />
        <Setter Property="Padding" Value="10,6" />
        <Setter Property="Background" Value="#17131B" />
        <Setter Property="Foreground" Value="#FFFFFFFF" />
        <Setter Property="BorderBrush" Value="#3E3343" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="FrameworkElement.Cursor" Value="Hand" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="{x:Type Button}">
              <Border Name="ButtonBorder" Background="{TemplateBinding Control.Background}" BorderBrush="{TemplateBinding Control.BorderBrush}" BorderThickness="{TemplateBinding Control.BorderThickness}" Padding="{TemplateBinding Control.Padding}">
                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
              </Border>
              <ControlTemplate.Triggers>
                <Trigger Property="UIElement.IsMouseOver" Value="True">
                  <Setter TargetName="ButtonBorder" Property="Border.BorderBrush" Value="#A85B9B" />
                  <Setter TargetName="ButtonBorder" Property="Border.Background" Value="#261D29" />
                  <Setter TargetName="ButtonBorder" Property="UIElement.Effect">
                    <Setter.Value>
                      <DropShadowEffect Color="#742965" BlurRadius="10" ShadowDepth="0" Opacity="0.65" />
                    </Setter.Value>
                  </Setter>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                  <Setter TargetName="ButtonBorder" Property="Border.Background" Value="#4A1D46" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                  <Setter TargetName="ButtonBorder" Property="UIElement.Opacity" Value="0.38" />
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>
      <Style x:Key="{x:Type TextBox}" TargetType="{x:Type TextBox}">
        <Setter Property="Margin" Value="3" />
        <Setter Property="Padding" Value="7,6" />
        <Setter Property="Background" Value="#09080B" />
        <Setter Property="Foreground" Value="#F5F1F6" />
        <Setter Property="BorderBrush" Value="#493E4F" />
        <Setter Property="TextBoxBase.CaretBrush" Value="#B45A9F" />
      </Style>
      <Style x:Key="{x:Type ComboBox}" TargetType="{x:Type ComboBox}">
        <Setter Property="MinHeight" Value="34" />
        <Setter Property="Margin" Value="3" />
        <Setter Property="Padding" Value="7,5" />
        <Setter Property="Background" Value="#151017" />
        <Setter Property="Foreground" Value="#FFFFFFFF" />
        <Setter Property="BorderBrush" Value="#493E4F" />
        <Setter Property="ItemContainerStyle">
          <Setter.Value>
            <Style TargetType="{x:Type ComboBoxItem}">
              <Setter Property="Foreground" Value="#E9E3EB" />
              <Setter Property="Background" Value="#111015" />
              <Setter Property="Padding" Value="10,7" />
              <Setter Property="HorizontalContentAlignment" Value="Stretch" />
              <Setter Property="Template">
                <Setter.Value>
                  <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                    <Border Name="ItemBorder" Background="{TemplateBinding Control.Background}" BorderBrush="#00FFFFFF" BorderThickness="3,0,0,0" Padding="{TemplateBinding Control.Padding}">
                      <ContentPresenter />
                    </Border>
                    <ControlTemplate.Triggers>
                      <Trigger Property="UIElement.IsMouseOver" Value="True">
                        <Setter TargetName="ItemBorder" Property="Border.Background" Value="#211821" />
                      </Trigger>
                      <Trigger Property="ListBoxItem.IsSelected" Value="True">
                        <Setter TargetName="ItemBorder" Property="Border.Background" Value="#4A1D46" />
                        <Setter TargetName="ItemBorder" Property="Border.BorderBrush" Value="#B05A9F" />
                        <Setter Property="Foreground" Value="#FFFFFFFF" />
                      </Trigger>
                    </ControlTemplate.Triggers>
                  </ControlTemplate>
                </Setter.Value>
              </Setter>
            </Style>
          </Setter.Value>
        </Setter>
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="{x:Type ComboBox}">
              <Grid>
                <Border Name="ComboBorder" Background="{TemplateBinding Control.Background}" BorderBrush="{TemplateBinding Control.BorderBrush}" BorderThickness="1">
                  <Grid>
                    <ToggleButton Focusable="False" Background="#00FFFFFF" BorderThickness="0" IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                      <Control.Template>
                        <ControlTemplate TargetType="{x:Type ToggleButton}">
                          <Border Background="#00FFFFFF">
                            <Path Data="M0,0 L5,5 L10,0" Fill="#A85B9B" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0" />
                          </Border>
                        </ControlTemplate>
                      </Control.Template>
                    </ToggleButton>
                    <ContentPresenter Name="SelectionPresenter" HorizontalAlignment="{TemplateBinding Control.HorizontalContentAlignment}" IsHitTestVisible="False" Margin="8,0,30,0" VerticalAlignment="Center" Content="{TemplateBinding ComboBox.SelectionBoxItem}" ContentTemplate="{TemplateBinding ComboBox.SelectionBoxItemTemplate}" ContentTemplateSelector="{TemplateBinding ItemsControl.ItemTemplateSelector}" />
                    <TextBox Name="PART_EditableTextBox" Visibility="Hidden" Background="#00FFFFFF" BorderThickness="0" Margin="4,0,30,0" VerticalContentAlignment="Center" Foreground="#FFFFFFFF" IsReadOnly="{TemplateBinding ComboBox.IsReadOnly}" />
                    <Border Height="2" Background="#6B245E" VerticalAlignment="Bottom" Margin="1,0" IsHitTestVisible="False" />
                  </Grid>
                </Border>
                <Popup Name="PART_Popup" Placement="Bottom" AllowsTransparency="True" Focusable="False" IsOpen="{TemplateBinding ComboBox.IsDropDownOpen}">
                  <Border Background="#0B090D" BorderBrush="#5A3154" BorderThickness="1" MinWidth="{TemplateBinding FrameworkElement.ActualWidth}" MaxWidth="{TemplateBinding FrameworkElement.ActualWidth}" MaxHeight="320">
                    <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
                      <ItemsPresenter />
                    </ScrollViewer>
                  </Border>
                </Popup>
              </Grid>
              <ControlTemplate.Triggers>
                <Trigger Property="UIElement.IsMouseOver" Value="True">
                  <Setter TargetName="ComboBorder" Property="Border.BorderBrush" Value="#87517E" />
                </Trigger>
                <Trigger Property="UIElement.IsKeyboardFocusWithin" Value="True">
                  <Setter TargetName="ComboBorder" Property="Border.BorderBrush" Value="#B05A9F" />
                  <Setter TargetName="ComboBorder" Property="UIElement.Effect">
                    <Setter.Value>
                      <DropShadowEffect Color="#742965" BlurRadius="9" ShadowDepth="0" Opacity="0.5" />
                    </Setter.Value>
                  </Setter>
                </Trigger>
                <Trigger Property="ComboBox.IsEditable" Value="True">
                  <Setter TargetName="SelectionPresenter" Property="Visibility" Value="Hidden" />
                  <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                  <Setter TargetName="ComboBorder" Property="UIElement.Opacity" Value="0.38" />
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>
      <Style x:Key="{x:Type ListView}" TargetType="{x:Type ListView}">
        <Setter Property="Background" Value="#0A090C" />
        <Setter Property="Foreground" Value="#FFFFFFFF" />
        <Setter Property="BorderBrush" Value="#403545" />
      </Style>
      <Style x:Key="{x:Type ListViewItem}" TargetType="{x:Type ListViewItem}">
        <Setter Property="Background" Value="#00FFFFFF" />
        <Setter Property="BorderBrush" Value="#00FFFFFF" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Margin" Value="1" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="{x:Type ListViewItem}">
              <Border Name="ItemBorder" Background="{TemplateBinding Control.Background}" BorderBrush="{TemplateBinding Control.BorderBrush}" BorderThickness="{TemplateBinding Control.BorderThickness}">
                <ContentPresenter />
              </Border>
              <ControlTemplate.Triggers>
                <Trigger Property="UIElement.IsMouseOver" Value="True">
                  <Setter TargetName="ItemBorder" Property="Border.Background" Value="#221A22" />
                </Trigger>
                <Trigger Property="ListBoxItem.IsSelected" Value="True">
                  <Setter TargetName="ItemBorder" Property="Border.Background" Value="#351832" />
                  <Setter TargetName="ItemBorder" Property="Border.BorderBrush" Value="#A84F96" />
                  <Setter TargetName="ItemBorder" Property="UIElement.Effect">
                    <Setter.Value>
                      <DropShadowEffect Color="#7A2B6C" BlurRadius="12" ShadowDepth="0" Opacity="0.7" />
                    </Setter.Value>
                  </Setter>
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>
      <Style x:Key="{x:Type DataGrid}" TargetType="{x:Type DataGrid}">
        <Setter Property="Background" Value="#0A090C" />
        <Setter Property="Foreground" Value="#F5F1F6" />
        <Setter Property="BorderBrush" Value="#493E4F" />
        <Setter Property="DataGrid.GridLinesVisibility" Value="Horizontal" />
        <Setter Property="DataGrid.HorizontalGridLinesBrush" Value="#3B303F" />
        <Setter Property="DataGrid.VerticalGridLinesBrush" Value="#3B303F" />
        <Setter Property="DataGrid.RowBackground" Value="#0D0B0F" />
        <Setter Property="DataGrid.AlternatingRowBackground" Value="#151119" />
        <Setter Property="DataGrid.HeadersVisibility" Value="Column" />
        <Setter Property="DataGrid.CanUserAddRows" Value="False" />
        <Setter Property="DataGrid.SelectionUnit" Value="FullRow" />
        <Setter Property="DataGrid.RowHeaderWidth" Value="0" />
      </Style>
      <Style x:Key="{x:Type DataGridColumnHeader}" TargetType="{x:Type DataGridColumnHeader}">
        <Setter Property="Background" Value="#211821" />
        <Setter Property="Foreground" Value="#F7F1F8" />
        <Setter Property="BorderBrush" Value="#5A3154" />
        <Setter Property="BorderThickness" Value="0,0,1,1" />
        <Setter Property="Padding" Value="9,7" />
        <Setter Property="FontWeight" Value="SemiBold" />
      </Style>
      <Style x:Key="{x:Type DataGridRow}" TargetType="{x:Type DataGridRow}">
        <Setter Property="Foreground" Value="#F5F1F6" />
        <Setter Property="BorderBrush" Value="#342B38" />
        <Style.Triggers>
          <Trigger Property="UIElement.IsMouseOver" Value="True">
            <Setter Property="Background" Value="#241B25" />
          </Trigger>
          <Trigger Property="DataGridRow.IsSelected" Value="True">
            <Setter Property="Background" Value="#4A1D46" />
            <Setter Property="Foreground" Value="#FFFFFFFF" />
          </Trigger>
        </Style.Triggers>
      </Style>
      <Style x:Key="{x:Type DataGridCell}" TargetType="{x:Type DataGridCell}">
        <Setter Property="Foreground" Value="{Binding Path=Foreground, RelativeSource={RelativeSource AncestorType=DataGridRow}}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="7,4" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Style.Triggers>
          <Trigger Property="DataGridCell.IsSelected" Value="True">
            <Setter Property="Background" Value="#00FFFFFF" />
            <Setter Property="Foreground" Value="#FFFFFFFF" />
          </Trigger>
        </Style.Triggers>
      </Style>
      <ControlTemplate x:Key="VerticalScrollBarTemplate" TargetType="{x:Type ScrollBar}">
        <Grid Width="10" Background="#0A090C">
          <Track Name="PART_Track" IsDirectionReversed="True">
            <Track.DecreaseRepeatButton>
              <RepeatButton Command="ScrollBar.PageUpCommand" Opacity="0" />
            </Track.DecreaseRepeatButton>
            <Track.Thumb>
              <Thumb Background="#5A3154" MinHeight="34">
                <Control.Template>
                  <ControlTemplate TargetType="{x:Type Thumb}">
                    <Border Name="ThumbBody" Background="{TemplateBinding Control.Background}" CornerRadius="4" Margin="2,1" />
                    <ControlTemplate.Triggers>
                      <Trigger Property="UIElement.IsMouseOver" Value="True">
                        <Setter TargetName="ThumbBody" Property="Border.Background" Value="#A15491" />
                      </Trigger>
                      <Trigger Property="Thumb.IsDragging" Value="True">
                        <Setter TargetName="ThumbBody" Property="Border.Background" Value="#C56AAF" />
                      </Trigger>
                    </ControlTemplate.Triggers>
                  </ControlTemplate>
                </Control.Template>
              </Thumb>
            </Track.Thumb>
            <Track.IncreaseRepeatButton>
              <RepeatButton Command="ScrollBar.PageDownCommand" Opacity="0" />
            </Track.IncreaseRepeatButton>
          </Track>
        </Grid>
      </ControlTemplate>
      <ControlTemplate x:Key="HorizontalScrollBarTemplate" TargetType="{x:Type ScrollBar}">
        <Grid Height="10" Background="#0A090C">
          <Track Name="PART_Track">
            <Track.DecreaseRepeatButton>
              <RepeatButton Command="ScrollBar.PageLeftCommand" Opacity="0" />
            </Track.DecreaseRepeatButton>
            <Track.Thumb>
              <Thumb Background="#5A3154" MinWidth="34">
                <Control.Template>
                  <ControlTemplate TargetType="{x:Type Thumb}">
                    <Border Name="ThumbBody" Background="{TemplateBinding Control.Background}" CornerRadius="4" Margin="1,2" />
                    <ControlTemplate.Triggers>
                      <Trigger Property="UIElement.IsMouseOver" Value="True">
                        <Setter TargetName="ThumbBody" Property="Border.Background" Value="#A15491" />
                      </Trigger>
                      <Trigger Property="Thumb.IsDragging" Value="True">
                        <Setter TargetName="ThumbBody" Property="Border.Background" Value="#C56AAF" />
                      </Trigger>
                    </ControlTemplate.Triggers>
                  </ControlTemplate>
                </Control.Template>
              </Thumb>
            </Track.Thumb>
            <Track.IncreaseRepeatButton>
              <RepeatButton Command="ScrollBar.PageRightCommand" Opacity="0" />
            </Track.IncreaseRepeatButton>
          </Track>
        </Grid>
      </ControlTemplate>
      <Style x:Key="{x:Type ScrollBar}" TargetType="{x:Type ScrollBar}">
        <Setter Property="Template" Value="{StaticResource VerticalScrollBarTemplate}" />
        <Style.Triggers>
          <Trigger Property="ScrollBar.Orientation" Value="Horizontal">
            <Setter Property="Template" Value="{StaticResource HorizontalScrollBarTemplate}" />
          </Trigger>
        </Style.Triggers>
      </Style>
      <Style x:Key="{x:Type ScrollViewer}" TargetType="{x:Type ScrollViewer}">
        <Setter Property="Background" Value="#0A090C" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="{x:Type ScrollViewer}">
              <Grid Background="{TemplateBinding Control.Background}">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="*" />
                  <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                  <RowDefinition Height="*" />
                  <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <ScrollContentPresenter Name="PART_ScrollContentPresenter" Grid.Row="0" Grid.Column="0" Content="{TemplateBinding ContentControl.Content}" ContentTemplate="{TemplateBinding ContentControl.ContentTemplate}" CanContentScroll="{TemplateBinding ScrollViewer.CanContentScroll}" />
                <ScrollBar Name="PART_VerticalScrollBar" Grid.Row="0" Grid.Column="1" Orientation="Vertical" Maximum="{TemplateBinding ScrollViewer.ScrollableHeight}" ViewportSize="{TemplateBinding ScrollViewer.ViewportHeight}" Visibility="{TemplateBinding ScrollViewer.ComputedVerticalScrollBarVisibility}" Value="{Binding VerticalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}" />
                <ScrollBar Name="PART_HorizontalScrollBar" Grid.Row="1" Grid.Column="0" Orientation="Horizontal" Maximum="{TemplateBinding ScrollViewer.ScrollableWidth}" ViewportSize="{TemplateBinding ScrollViewer.ViewportWidth}" Visibility="{TemplateBinding ScrollViewer.ComputedHorizontalScrollBarVisibility}" Value="{Binding HorizontalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}" />
                <Border Grid.Row="1" Grid.Column="1" Background="#0A090C" />
              </Grid>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>
      <Style x:Key="{x:Type UserControl}" TargetType="{x:Type UserControl}">
        <Setter Property="FontFamily" Value="Segoe UI, Microsoft YaHei UI" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Foreground" Value="#EDE7EF" />
        <Setter Property="FrameworkElement.UseLayoutRounding" Value="True" />
      </Style>
      <Style x:Key="{x:Type CheckBox}" TargetType="{x:Type CheckBox}">
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Foreground" Value="#EDE7EF" />
        <Setter Property="Background" Value="#1B1720" />
        <Setter Property="BorderBrush" Value="#46384B" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="14,10" />
        <Setter Property="Margin" Value="0,4,8,4" />
        <Setter Property="MinHeight" Value="42" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="FrameworkElement.Cursor" Value="Hand" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="{x:Type CheckBox}">
              <Border Name="Card" CornerRadius="6" Padding="{TemplateBinding Control.Padding}" Background="{TemplateBinding Control.Background}" BorderBrush="{TemplateBinding Control.BorderBrush}" BorderThickness="{TemplateBinding Control.BorderThickness}">
                <Grid>
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3" />
                    <ColumnDefinition Width="*" />
                  </Grid.ColumnDefinitions>
                  <Border Name="StateBar" CornerRadius="1.5" Background="#5D4C63" />
                  <ContentPresenter Grid.Column="1" Margin="10,0,0,0" RecognizesAccessKey="True" VerticalAlignment="Center" HorizontalAlignment="{TemplateBinding Control.HorizontalContentAlignment}">
                    <FrameworkElement.Resources>
                      <ResourceDictionary>
                        <Style x:Key="{x:Type TextBlock}" TargetType="{x:Type TextBlock}">
                          <Setter Property="TextWrapping" Value="Wrap" />
                        </Style>
                      </ResourceDictionary>
                    </FrameworkElement.Resources>
                  </ContentPresenter>
                </Grid>
              </Border>
              <ControlTemplate.Triggers>
                <Trigger Property="ToggleButton.IsChecked" Value="True">
                  <Setter TargetName="Card" Property="Border.Background" Value="#41203D" />
                  <Setter TargetName="Card" Property="Border.BorderBrush" Value="#B66AA6" />
                  <Setter TargetName="StateBar" Property="Border.Background" Value="#F2A0D9" />
                </Trigger>
                <Trigger Property="ToggleButton.IsChecked" Value="{x:Null}">
                  <Setter TargetName="StateBar" Property="Border.Background" Value="#CDA6C8" />
                </Trigger>
                <Trigger Property="UIElement.IsMouseOver" Value="True">
                  <Setter TargetName="Card" Property="Border.BorderBrush" Value="#D28CBE" />
                </Trigger>
                <Trigger Property="UIElement.IsKeyboardFocused" Value="True">
                  <Setter TargetName="Card" Property="Border.BorderBrush" Value="#F3BAE2" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                  <Setter TargetName="Card" Property="UIElement.Opacity" Value="0.45" />
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>
      <Style x:Key="{x:Type TabItem}" TargetType="{x:Type TabItem}">
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FontWeight" Value="Medium" />
        <Setter Property="Foreground" Value="#BFB5C2" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="{x:Type TabItem}">
              <Border Name="TabSurface" Background="#18131B" BorderBrush="#403545" BorderThickness="1,1,1,0" Padding="12,9" Margin="0,0,3,0" CornerRadius="5,5,0,0">
                <ContentPresenter ContentSource="Header" />
              </Border>
              <ControlTemplate.Triggers>
                <Trigger Property="TabItem.IsSelected" Value="True">
                  <Setter TargetName="TabSurface" Property="Border.Background" Value="#4A1D46" />
                  <Setter Property="Foreground" Value="#FFFFFFFF" />
                </Trigger>
                <Trigger Property="UIElement.IsMouseOver" Value="True">
                  <Setter TargetName="TabSurface" Property="Border.BorderBrush" Value="#B45A9F" />
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>
      <Style x:Key="{x:Type TabControl}" TargetType="{x:Type TabControl}">
        <Setter Property="Background" Value="#121016" />
        <Setter Property="Foreground" Value="#EDE7EF" />
        <Setter Property="BorderBrush" Value="#403545" />
        <Setter Property="Padding" Value="0" />
      </Style>
    </ResourceDictionary>
  </Application.Resources>
</Application>