## What this changes

One or two sentences. If it changes something on screen, say which page.

## Why

## How it was checked

- [ ] `build.cmd` is clean (around 165 `CS8632` warnings, zero errors)
- [ ] `publish.cmd` produces `dist\FGOAC scooby.exe` and it starts
- [ ] the pages the change touches were opened and looked at
- [ ] if a translation or data file changed: no CJK left where English was expected, and every
      number still matches the Japanese original

## Notes

Anything a reviewer would otherwise have to work out for themselves: a value that looks wrong but is
not, something deliberately left alone, a follow-up this does not do.
